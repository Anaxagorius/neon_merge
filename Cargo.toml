[package]
name = "neon_merge"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.15"

# Faster iterative compile in debug
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

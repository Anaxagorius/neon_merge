[package]
name = "neon_merge"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.15"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
rand = "0.8"

# Faster iterative compile in debug
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
